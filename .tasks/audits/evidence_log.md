# Grounding Hardening — Evidence Log

Per-task verification evidence (VERIFICATION_PROTOCOL §2.5). Trace IDs are in the
`pipeline_traces` table; view via `GET /api/v1/traces/{id}`.

| Task | Trace ID | Verdict | Evidence |
|------|----------|---------|----------|
| Bootstrap §1 | `783359ef-169b-4074-8509-32f2bc107c72` | PASS | HTTP 200, Georgian answer cites admin offences code + fine amounts for 40 km/h; trace completed, 13 steps. Baseline export saved. |
| 0.1 per-collection retrieval | `f4e8450b-7197-4976-b1fd-e73a28b6e5b4` | PASS | `rag_vector_search.per_collection_counts` = laws 240 / court 150 / chamber 60 (audit baseline: laws 1). `rag_rerank.pool_per_collection` = laws 50 / court 40 / chamber 10. `rag_final_selection` contains `labour_code.chapter_X.article_47.chunk_13` and `article_48.chunk_8`; final = 16 laws + 8 court. Required a second fix: balanced rerank candidate pool (`_balance_rerank_pool`) — first run (trace `2e4d80e5`) showed top-100 pre-rerank pool was 88 court / 1 law. Unit: `TestPerCollectionVectorSearch`, `TestBalancedRerankPool`. |
| 0.2 deadlines rule | `f4e8450b-7197-4976-b1fd-e73a28b6e5b4` | PASS (partial precision) | Response states the 10-day employer answer deadline (grounded, art 62) and the ~1-month court filing deadline with an explicit caution to verify. Exact statutory "30 კალენდარული დღე" wording expected to become deterministic after 1.3 full-code injection (the 30-day paragraph chunk of art 48 was not among retrieved chunks). Unit: `TestPromptGroundingRules`. |
| 0.3 threshold domain gate | `f4e8450b-7197-4976-b1fd-e73a28b6e5b4` | PASS | `threshold_lookup` step: `query_domains=['labor']`, injected=`['labor_notice_period']`, gated_out_count=35. Zero criminal threshold chunks in context (audit baseline: 3 criminal entries at distance 0.0). Unit: `TestThresholdDomainGate`. |
| 0.4 court-case citation duty | `f4e8450b-7197-4976-b1fd-e73a28b6e5b4` | PASS | Response cites `ას-792-2019`, `ას-536-2021` next to practice-based claims (audit baseline: 5 cases used, 0 cited). Verification of case numbers against corpus = Phase 3.1. |
| 0.5 sub-article extraction | unit-level | PASS | `მუხლი 48.8`, `48-ე მუხლის მე-8 ნაწილი`, `48-ე მუხლის პირველი ნაწილი` all extract article+paragraph; sentence-final dots not misparsed. Unit: `TestSubArticleExtraction` (7 tests). Citations now carry a `paragraph` field. |

Full suite after Phase 0: **488 passed / 2 known pre-existing failures** (test_agy_verification, needs host:5432 postgres from another project).

| Task | Trace ID | Verdict | Evidence |
|------|----------|---------|----------|
| 1.1 article store (mechanism A) | unit-level + store build | PASS | `law_corpus/scripts/build_article_store.py` → `article_store.db` (12 codes, 5,325 articles, 18,314 paragraphs, 48.7 MB, FTS5). Wired into `run_full_pipeline.sh` (step 6) + `run_incremental_update.sh`. `get_article("labour_code","48")` returns full text, **9 paragraphs**, anchor `#article_48`, and the 30-day text. NOTE: parsed JSON already had `paragraphs`/`cross_references` populated (CONTEXT.md said empty — stale); no parser change was needed. Unit: `test_article_store_service.py` (14 tests). |
| 1.2 navigation tools (mechanism B) | `34105c17-8c88-432a-af39-d6e5a1e436b2` | PASS | Exact-text Q on Election Code art 79 → trace shows `llm_requested_tools` → `article_store_lookup` (found=true, election_code.article_79) → `tool_executed: get_article` with matsne URL; answer quotes exact text. `get_article`/`browse_code` in ALWAYS/CASE_CREATION/FULL_CASE tool groups; prompt rule 10 added. Note: `rag_config` cannot actually disable georgian_laws (`to_collection_names` falls back to it), so the plan's "disable laws collection" scenario was replaced with an obscure-article exact-text probe. Unit: `test_grounding_phase1.py`. |
| 1.3 whole-code injection (mechanism C) | `b6f13aa5-a5f7-4503-9271-631473123ec3` | PASS | G1 with `FULL_CODE_INJECTION=true`: `full_code_injected` step (labour_code, 90 articles, 109,871 chars, domain=labor). Response cites **art 47 AND art 48**, exact 30-day deadline (`30 კალენდარული დღის`), case `ას-1280-2019` (the audit's compensation case), `citation_verification.not_found=[]`, zero Latin outside URLs. Flag default **false** → prod no-op. Whitelist: labour_code + constitution only (admin_offences=763k chars, too big — deviation from plan's assumption, documented). Court practice + threshold chunks stay via RAG; statute chunks dropped when injected. |
| 2.1 retrieval-repair | unit + `d07667f5` run path | PASS (unit-proven) | `_phase_3_verify`: corpus_found citations now fetch FULL article text from the store, emit `retrieval_repair` step, and force a Flash confirm/correct pass (previously accepted silently — the audit's "verified by luck" hole). Unit: `TestRetrievalRepair`. E2E firing occurs whenever a citation is outside context; observed corrections in `citation_correction` steps during probes. |
| 2.2 anchored claims | `d07667f5-1241-4719-89f1-14f82cab12d9` | PASS | `anchoring_check` step live on every response (claim paragraphs, unanchored rate, samples). Flag-only by design — automatic stripping of Georgian legal text deemed riskier than surfacing the metric (deviation from plan's "flags/strips", documented). Prompt anchoring duty added (rule 11 / advocate rule 8). |
| 2.3 faithfulness pass | `d07667f5-1241-4719-89f1-14f82cab12d9` | PASS | `FAITHFULNESS_CHECK=true` locally (default false → prod no-op). E2E on G2: `faithfulness_check` flagged a genuinely unsupported claim (საურავი penalty escalation not in context) and `faithfulness_correction` rewrote the response. Unit: seeded-hallucination tests in `TestFaithfulnessPass`. |
| 2.4 uncertainty policy | unit-level | PASS | Statute-type question with zero statute grounding (no chunks, no tool hit, no injection) → response prefixed with explicit Georgian disclaimer + `uncertainty_disclaimer_added` step. Unit: `TestUncertaintyPolicy` (5 tests, incl. forced-empty). |
| 3.1 case-citation verification | unit-level (+ hallucination proven in corpus: `ას-792-2019` cited in trace `f4e8450b` response is NOT in corpus) | PASS | `extract_case_citations` (ას/ბს/გს/აპ/აგ/კოლ forms) + `verify_case_citations` against court collections' `case_id` metadata; hallucinated numbers → `case_citation_verification` step + Flash correction. Unit: `test_grounding_phase3.py`. |
| 3.2 sub-article verification | unit-level | PASS | Cited paragraph ("48.8") checked against the store's paragraph list; nonexistent paragraph → `subarticle_verification` issue + correction (e.g. 48.99 flagged, available 1–9). |
| 3.3 live drift sampling | script run 2026-07-19 | PASS | `law_corpus/scripts/drift_check.py`: 5/5 sampled articles' text found byte-normalized on live matsne pages (report: `law_corpus/data/georgian_laws/drift_report.json`). Wired into weekly audit. |
| 4.1 grounding metrics | live endpoint | PASS | `GET /api/v1/traces/metrics/grounding?days=N` + dashboard panel ("Grounding (7d)"). Live snapshot on 12 recent traces: statute grounding 100%, citations clean 100%, deadline coverage 83%. Computed on demand from traces (no separate nightly job needed — deviation documented). Unit: `test_grounding_metrics.py`. |
| 4.2 golden suite | `eval/golden_retrieval.yaml` + runner | PASS | 30 questions across 10 domains; **every** expected article validated against the article store (existence + topic keyword + matsne URL recorded in the YAML). Runner `eval/run_golden_retrieval.py` drives the live pipeline and scores C1–C8 per trace; CI gate configurable (`--checks C2`). |
| 4.3 weekly auto-audit | scaffold | PASS | `eval/run_weekly_audit.sh`: golden subset run + 7-day metrics snapshot + drift check → findings filed to `.tasks/audits/auto_audit_<date>.md`. Scheduling left to cron/launchd (documented). |
