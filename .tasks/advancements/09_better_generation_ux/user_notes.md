# User Notes: Better Generation UX

- "Can we have streamed generation, showing user whats being generated, rather sending the full answer once ready. before we should do quering law, thinking, and etc when it has content write through stream"

*Note: Ensure that the UI clearly communicates the non-generating phases (retrieval, expansion, reranking) as "querying law" or "thinking" before the text stream starts.*
